import { ROUTES } from "@/shared/constants/routes";

export const AGENT_DOCS_DIRECTIVE_TEXT =
  `For the complete documentation index, see llms.txt. Markdown variants are available at explicit .md URLs. An agent skill is available at ${ROUTES.AGENT_SKILLS_SITE_SKILL}.` as const;
